{
  "name": "interconnect-my-own-whatsapp-web",
  "version": "1.0.0",
  "description": "**InterConnect - A Seamless Chatting Experience**",
  "main": "index.js",
  "scripts": {
    "start": "npm install concurrently --save-dev && concurrently \"npm i && cd ./server/ && npm i && npm start \" \"cd ./client/ && npm i && npm start \" \" cd ./socket/ && npm i  && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"cd ./client/ && npm i && npm run build "
    
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
